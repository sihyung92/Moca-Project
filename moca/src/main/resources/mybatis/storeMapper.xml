<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkssj.moca.model.StoreDao">
  <select id="selectByTag" parameterType="Map" resultType="com.kkssj.moca.model.entity.StoreVo">
	SELECT *, st_distance_sphere(point(xLocation, yLocation), point(#{x}, #{y})) "distance" FROM STORE WHERE TAG LIKE CONCAT('%',#{keyword},'%')
	<if test="region!=null">
		AND ADDRESS LIKE CONCAT('%',#{region},'%')
	</if>
	ORDER BY DISTANCE DESC
  </select>
  <select id="selectByKakaoId" parameterType="INTEGER" resultType="com.kkssj.moca.model.entity.StoreVo">
  	SELECT store_id, tag, reviewCnt, viewCnt, (tasteLevel+priceLevel+serviceLevel+moodLevel+convenienceLevel)/5 "averageLevel" FROM STORE WHERE KAKAOID=#{id}
  </select>
  <select id="selectByRegion" parameterType="String" resultType="java.util.Properties">
	SELECT * FROM COORDINATES WHERE REGION = #{value}  
  </select>
</mapper>